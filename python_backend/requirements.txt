# HYBA FULLSTACK / PYTHIA-PULVINI Dependencies
# Production-grade mathematical mining substrate

# Core Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# Async HTTP
aiohttp==3.10.10

# Numerical & Scientific Computing
numpy==1.26.4
scipy==1.14.1

# Authentication & Security
argon2-cffi==23.1.0
PyJWT==2.9.0
cryptography==43.0.1

# Database & Migrations
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.10

# Monitoring & Observability
prometheus-client==0.21.0
python-json-logger==2.0.7
psutil==6.1.0

# HTTP Client
requests==2.32.3

# Apple Silicon ML (optional for Metal acceleration)
mlx==0.20.0; platform_system == "Darwin" and platform_machine == "arm64"

# Noise generation for testing
noise==1.2.2

# Command-line argument parsing (if needed beyond stdlib argparse)
# argparse is stdlib, no need to include

opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp==1.29.0

# Distributed systems & caching
redis==5.0.1
aioredis==2.0.1
