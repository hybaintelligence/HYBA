# Millennium Runtime Elevation Bridge

HYBA_FULLSTACK remains the separate funding/runtime repository.

This bridge extracts the seven-domain mathematical review frame from `HYBA_Unified_Backend` as local challenge contracts. It does not import or depend on the Backend at runtime.

## Added evidence layer

Files:

```text
scripts/generate_millennium_runtime_elevation_packet.py
tests/test_millennium_runtime_elevation_packet.py
scripts/run_millennium_elevation_gate.py
```

## Seven FULLSTACK challenge dimensions

```text
Riemann        -> spectral coherence / phi replay
P vs NP        -> search reduction / witness verification
Navier-Stokes  -> runtime flow smoothness
Yang-Mills     -> perturbation energy gap
Hodge          -> memory geometry and cycle evidence
BSD            -> resource-flow state gated by accepted-share evidence
Poincare       -> topological identity preservation
```

## Run

```bash
python scripts/run_millennium_elevation_gate.py
```

or directly:

```bash
python scripts/generate_millennium_runtime_elevation_packet.py
python -m pytest tests/test_millennium_runtime_elevation_packet.py -q
```

## Compatibility

This layer adds tests and artifacts only. It does not change production API routes, mining runtime flow, funding gate semantics, accepted-share requirements, Docker entrypoint, or operator credential validation.

## Canonical formulation

HYBA_FULLSTACK extracts the Millennium operationalisation frame as local challenge contracts, not as a runtime dependency. The funding engine remains separate. The science is elevated by seven-domain runtime checks, phi-ablation controls, forensic packet hashes, and replayable evidence artifacts.
