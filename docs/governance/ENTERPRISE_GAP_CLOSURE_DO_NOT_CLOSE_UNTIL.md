# Do Not Close Until

Do not close #130 until:

- the implementation PR is merged;
- the command set passes;
- the generated gap-closure transcript reports `passed: true`;
- remaining external dependencies are tracked outside the repository.
