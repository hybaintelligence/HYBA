# Unit Economics Evidence

## Status as of 2026-06-22

Unit economics are **documented_but_external_dependency** rather than closed. Agent D cannot fabricate measured AWS cost per 1000 QaaS executions because this branch does not include a verified live staging endpoint, AWS Cost Explorer output, or Agent C's committed `load_testing/results/baseline.json` results.

## Required calculation

```text
cost_per_1000_qaas_executions = (qaas_allocated_aws_cost_usd / qaas_execution_count) * 1000
```

Minimum evidence required:

1. `load_testing/results/baseline.json` with p50/p95/p99 latency, error rate, concurrency, and execution count.
2. AWS Cost Explorer report generated by `python scripts/finops_report.py` for the same test window or an agreed allocation window.
3. Commit SHA and command transcript for both inputs.

## Current placeholder control

Until real load and cost evidence exists, revenue projections in historical planning documents must be treated as projections, not proven production economics.
