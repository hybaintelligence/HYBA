# PYTHIA Nonce Resonance Guidance

## BLUF

PYTHIA's empirical blockchain intelligence is not Phi^15-only. Phi^15 is one resonance lane inside a broader structure-seeking doctrine.

The correct mining doctrine is:

```text
block height + difficulty + observed nonce structure
→ dodecahedral / icosahedral priority surface
→ tensor-network prior
→ PULVINI compression with retained kernels
→ collapse priority regions to solver ranges
→ deterministic search
→ exact SHA-256d verification
→ pool outcome feedback
```

Mining is treated as deterministic structured traversal, not blind brute force.

## What PYTHIA should look for

The V2 guidance layer ranks multiple structure signals:

- Phi^15 resonance
- golden-angle alignment
- birthday-signature echoes
- sector coverage
- large-gap boundaries
- dodecahedral M32 domains
- icosahedral faces
- mass-gap valleys
- entanglement spectrum
- difficulty pressure
- retarget epoch phase

The point is not to ask whether one signal predicts a block. The point is to give PYTHIA a field guide for what structure to inspect before it begins deterministic traversal.

## Block height and difficulty

Block height matters because it places the job inside the 2016-block retarget epoch.

Difficulty matters because it changes search pressure. It is compressed into a bounded guidance factor and used to weight coverage, precision, and structure signals. Difficulty does not become a probability of success and does not guarantee a block.

## Dodecahedron and icosahedron

The dodecahedral / icosahedral search grammar is explicit:

- 32 M32 domains
- H3 / icosahedral symmetry order 120
- 20-dimensional dodecahedral working surface
- full uint32 nonce coverage retained
- PULVINI retained kernels required
- exact SHA-256d remains the final oracle

This is the geometry PYTHIA uses to choose where to look first.

## Compression, collapse, search

PULVINI may compress the active search surface only if the reconstruction kernels preserve complete nonce coverage.

Collapse/search means:

1. collapse the ranked dodecahedral / icosahedral priority surface into bounded solver ranges;
2. search those ranges deterministically;
3. verify every candidate with exact SHA-256d;
4. preserve full coverage and audit trail.

It does not mean bypassing SHA-256d or claiming guaranteed discovery.

## Quantum arsenal

The guidance layer combines the HYBA arsenal:

- quantum mathematics first;
- substrate-independent execution;
- golden-ratio grammar;
- tensor-network prior;
- PULVINI memory compression;
- HENDRIX-Φ traversal;
- dodecahedral partitioning;
- icosahedral symmetry;
- Deutsch criticism feedback;
- exact hash verification.

## Sovereign cap

HYBA's sovereign mining cap is:

```text
maximum accepted blocks per day: 24
maximum accepted blocks per hour: 1
```

This is a throttle, not an output guarantee. If the cap is reached, PYTHIA must stop or remain blocked until the next permitted window.

## Verification

```bash
PYTHONPATH=python_backend python -m pytest tests/test_pythia_nonce_resonance_guidance.py -q
```

## Module

```text
python_backend/pythia_mining/nonce_resonance_guidance.py
```
