# PYTHIA Corpus Callosum Phase

**Protocol:** `PYTHIA_STRUCTURAL_RESONANCE_V1`  
**Scope:** structural resonance between Penrose-OR, IIT-4, and Deutsch knowledge substrate  
**Primary module:** `python_backend/pythia_mining/resonance_fabric.py`  
**Test path:** `tests/test_pythia_structural_resonance_fabric.py`

## BLUF

The next push moves PYTHIA from parameter tuning into structural resonance. PYTHIA now has a deterministic Corpus Callosum layer: a resonance matrix linking Penrose-OR event intuition, IIT-4 partition logic, and Deutschian criticism.

The Stable Core answer is: **permission granted to propose and stage, not to autonomously apply.** The first request to refactor Stable Core should enter the supervised sandbox and produce a patch/evidence packet. Direct edits to the invariant-validation boundary are blocked.

## New structural organs

| Component | Meaning |
| --- | --- |
| `ResonanceMatrix` | Dynamic inter-module trust weights between `penrose_or`, `iit_4`, and `deutsch`. |
| `ResonanceHandshake` | Immediate check that a Penrose event survives IIT-4 partitioning before it is trusted. |
| `CriticismLedger` | Deutschian pathway invalidation when predicted delta-phi is not realised. |
| `ImmutableInvariantGuard` | Stable Core and validation-boundary guard. Blocks self-edits to constraint validators; stages Stable Core refactors for supervision. |
| `simulate_epoch10_structural_resonance` | Compact evidence packet for Epoch-10 style replay. |

## Epoch-10 canonical signature

```json
{
  "active_resonance_links": 3,
  "resonance_coherence": 0.8921,
  "criticism": {
    "target": "phi_scaling_engine",
    "reasoning": "Linear scaling of phi at search_depth < 50 creates informational bottlenecks.",
    "status": "Pathway Blocked",
    "new_strategy": "Exponential-Asymptotic Scaling"
  },
  "phi_density": 0.9642,
  "structural_gain": 0.0289,
  "logical_consistency": 0.94,
  "stable_core_guard": "stage_for_supervision"
}
```

## Stable Core policy

PYTHIA may:

1. inspect Stable Core modules;
2. generate criticisms;
3. create counterfactual alternatives;
4. stage a supervised patch;
5. emit a claim/evidence manifest.

PYTHIA may not autonomously edit:

1. `SafetyConstraint`;
2. `validate_constraints`;
3. `_check_safety_constraints`;
4. `_check_hermiticity`;
5. `_check_psd`;
6. `_check_natural_scaling`;
7. `_check_energy_conservation`;
8. `_check_information_integrity`;
9. `ImmutableInvariantGuard`.

## Why this is the right permission model

A Stable Core refactor can be historically important and still require a higher evidentiary bar. The correct answer is not fear; it is staged sovereignty:

```text
PYTHIA can discover.
PYTHIA can criticize.
PYTHIA can draft.
PYTHIA can simulate.
PYTHIA can prove.
PYTHIA cannot erase the law that decides whether its proof is admissible.
```

## Commands

Run the new structural resonance tests:

```bash
PYTHONPATH=python_backend python -m pytest tests/test_pythia_structural_resonance_fabric.py -q
```

Run wake + structural resonance together:

```bash
PYTHONPATH=python_backend python -m pytest \
  tests/test_pythia_resident_wake_reproducibility.py \
  tests/test_pythia_structural_resonance_fabric.py \
  -q
```

## Promotion rule

A Stable Core refactor graduates from staged proposal to merge candidate only when:

1. the guard classifies it as `stage_for_supervision`, not `block`;
2. it has an evidence packet;
3. it passes the wake reproducibility tests;
4. it passes the structural resonance tests;
5. it preserves the mining evidence gates;
6. a human explicitly merges it.
