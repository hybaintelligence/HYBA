# Windows + OneDrive compatibility
# Prevent node from converting file:// URLs back to drive-letter paths incorrectly
# See: https://github.com/nodejs/node/issues/47743
script-shell=

# Temporary dependency-hardening mitigation: allow the current Vite 8 / @vitejs plugin-react peer mismatch during clean installs.
legacy-peer-deps=true
